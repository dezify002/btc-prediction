{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "cd webapp && gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --timeout 60"
  }
}